import { Ionicons } from "@expo/vector-icons";

export const icon = {
    home: ({color}: {color: string}) => (
        <Ionicons name="home-outline" size={24} color={color} />
    ),
    profile: ({color}: {color: string}) => (
        <Ionicons name="home-outline" size={24} color={color} />
    ),
    recipe: ({color}: {color: string}) => (
        <Ionicons name="home-outline" size={24} color={color} />
    ),
};